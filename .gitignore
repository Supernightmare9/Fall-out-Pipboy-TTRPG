# Node.js dependencies
node_modules/

# Environment / secrets — never commit real credentials
.env

# Build / OS artifacts
dist/
.DS_Store
Thumbs.db
*.log

# Campaign data (auto-saved by server — use git to backup if desired)
# Keep the directory itself tracked via campaigns/.gitkeep
campaigns/*.json
